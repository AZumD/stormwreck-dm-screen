# Player static assets

Theme images for the player companion. Served as normal static files from the repo (not under `DM_DATA_ROOT`).

| File | Use |
|------|-----|
| `fairy-forest-bg.jpg` | Player app background (`css/player.css`) |
