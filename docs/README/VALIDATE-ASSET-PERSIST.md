# VALIDATE-ASSET-PERSIST.js

## Purpose
Checks that catalogue portraits/maps stay file-backed under `/data/assets`, and that empty sibling fields / ordinary saves do not delete image files.

## Run
`node test/validate-asset-persist.js`
