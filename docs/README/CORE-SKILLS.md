# CORE-SKILLS.js

## Purpose
Starter Skill catalogue seeds (ability checks). Data only — not hardcoded in app logic.

## File
`js/catalogue-seeds/core-skills.js` → `CatalogueSeeds.skill`

## Notes
- Stable ids: `skill-nature` with `linkId: "nature"` → `@skill:nature|Nature`
- Custom skills can be added in the Skill catalogue UI
- Merged via `CatalogueStore.mergeSeeds` (new ids + empty ref fields only)
