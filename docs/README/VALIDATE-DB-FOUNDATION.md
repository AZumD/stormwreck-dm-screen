# VALIDATE-DB-FOUNDATION.js

## Purpose
Static checks for Phase 1 Postgres foundation (schema, migration, seed, API wiring). Does not require a running database.

## Run
```bash
node test/validate-db-foundation.js
```

Included in `npm test`.
